import 'package:flutter/material.dart';

class DSColors {
  // Entradas
  static const incomeFixed = Color(0xFFA5D6A7);
  static const incomeVariable = Colors.green;

  // Despesas
  static const expenseFixed = Color(0xFFEF9A9A);
  static const expenseInstallment = Color(0xFFFFCC80);
  static const expenseVariable = Color(0xFFF44336);
}
